function openContainer() {
    var container = document.getElementById('items-form-container');
    var siteContent = document.getElementById('site-content');

    container.hidden = false;
    siteContent.hidden = true;
}